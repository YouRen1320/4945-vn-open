# 2.5 验收 · 后果与中点转折

> 状态：**前期选择回收 + 共享中点转折 + 中点后回应决策 + 内容修订迁移矩阵全部落地，单元/类型/剧情图/资产审计通过**。
> 方案：[SOL-PLAN-2.5.md](../../docs/SOL-PLAN-2.5.md)；合同见 `docs/season2/S2-EPISODE-2.5.md` / `S2-MIDPOINT-CONTRACT.md` / `S2-CONTENT-MIGRATION-MATRIX.md`。

## 1. 已交付

| 项 | 内容 | 证据 |
| --- | --- | --- |
| 剧情节点 | `s2-2.5.ts`：entry（按 s2Diverge 分流）→ consequence → org2..org6 变体 → midpoint → respond（2 选）→ closing → exit | `src/content/story/s2-2.5.ts` |
| 剧情图聚合 | 注册到 `story/index.ts`，2.5 完成哨兵加入终点白名单 | `src/content/story/index.ts` |
| 集注册表 | `s2-2.5`（index=3）entry/completion；payoff ledger 2 条 structural | `src/content/season2/registry.ts` |
| 选择回收 | 读 s2Diverge 分流后果；写入 s2Midpoint 供 2.6 | `s2-2.5.ts` + season2.test |
| 迁移矩阵 | 各完成点升级到 2.5 的恢复路径（沿用通用 episode 机制） | `S2-CONTENT-MIGRATION-MATRIX.md` |
| 单元测试 | 2.5 新增 6 项（registry 2 + store 4），全量 372 项通过 | 见 §3 |

## 2. 验收标准闭合（SOL-PLAN-2.5 §6）

| 项 | 状态 | 说明 |
| --- | --- | --- |
| §6.1 每条路线至少回收一个前期选择，结果可观察 | ✅ | s2Diverge 分流 + route 变体代价 |
| §6.2 中点由因果推动，改变后半季目标 | ✅ | 联合对峙 + 联席协调席，不依赖新事实 |
| §6.3 中点后有一次回应/决策，阶段收束完整 | ✅ | s2-2.5-respond 写入 s2Midpoint |
| §6.4 dueIn=2.5 项关闭或有延期记录 | ✅ | 2.3/2.4 选择已在 2.4/2.5 回收 |
| §6.5 2.2–2.4 完成点与 2.4 中途档可升级继续 | ✅ | 迁移矩阵 + store 测试覆盖 |
| §6.6 schemaVersion/contentRevision 分离，alias/tombstone 路径 | ✅ | 沿用机制；矩阵登记 |
| §6.7 第一季与全部已发布 episode 零回归，素材 ready | ✅ | 372 单测通过；零新增素材 |
| §6.8 类型/单测/图/构建/E2E/离线 | ✅ | 审计通过；E2E 随 3.0 |

## 3. 验证命令与结果

```bash
vue-tsc -b --pretty false        # 0 错误
vitest run                       # 33 文件 / 372 项全过（2.5 新增 6）
node scripts/audit-assets.mjs    # 通过，2.5 零新增托管视觉
npm run build                    # 成功
```

## 4. 有意不做（SOL-PLAN-2.5 §9）

- 不提前进入 2.7 危机或 2.8 高潮。
- 不用新 schema 掩盖迁移问题；不重写已发布选择含义。
- 不生成图片（本版零新增）。
