# 2.4 验收 · 首次实质分歧

> 状态：**评议现场 + 路线同期变体 + 实质分歧（confront/coalition）+ 持久状态继承机制全部落地，单元/类型/剧情图/资产审计通过；已发布至 4945.iyouren.top（v2.4.0，2026-08-05）**。
> 发布证据：`deploy/RELEASE-v2.4.0.md`；`/srv/4945-vn/current -> /srv/4945-vn/releases/2.4.0`；GAME_APP_VERSION=`2.4.0`；sw.js CACHE_VERSION=`4945-v2.4.0`。
> 方案：[SOL-PLAN-2.4.md](../../docs/SOL-PLAN-2.4.md)；episode 合同见 `docs/season2/S2-EPISODE-2.4.md`；分支预算见 `S2-BRANCH-BUDGET.md`。

## 1. 已交付

| 项 | 内容 | 证据 |
| --- | --- | --- |
| 剧情节点 | `s2-2.4.ts`：entry（按 s2Stance 分流）→ common → org2..org6 变体 → diverge（2 选）→ closing → exit | `src/content/story/s2-2.4.ts` |
| 剧情图聚合 | 注册到 `story/index.ts`，2.4 完成哨兵加入允许终点白名单 | `src/content/story/index.ts` |
| 集注册表 | `s2-2.4`（index=2）entry/completion；payoff ledger 2 条 structural | `src/content/season2/registry.ts` |
| 变量继承 | 2.4 读取 2.3 的 `s2Stance`，写入持久 `s2Diverge` 供 2.5 回收 | `s2-2.4.ts` effects + season2.test |
| 单元测试 | 2.4 新增 5 项（registry 2 + store 3），全量 369 项通过 | 见 §3 |

## 2. 验收标准闭合（SOL-PLAN-2.4 §6）

| 项 | 状态 | 说明 |
| --- | --- | --- |
| §6.1 每条活跃路线均有实质分歧，改变目标/合作/资源/代价/结构之一 | ✅ | confront/coalition 改变合作对象与代价 |
| §6.2 本集内展示不同即时结果，持久状态登记给 2.5 | ✅ | s2Diverge 持久，2.5 回收 |
| §6.3 无伪选择、无唯一正确答案 | ✅ | 两选项各有合理意图 |
| §6.4 branch budget / ledger / 节点图 / 路径数一致 | ✅ | 见 S2-BRANCH-BUDGET.md |
| §6.5 动态加载/离线（沿用 2.2/2.3 机制，无新增） | ✅ | 未改 sw/加载机制 |
| §6.6 已发布 episode ID/含义/完成状态不变 | ✅ | 仅追加 s2-2.4 |
| §6.7 正式素材 ready；第一季/2.2/2.3/schema5/v2 回归 | ✅ | 369 单测通过；零新增素材 |
| §6.8 类型/单测/图/构建/E2E/设备 | ✅ | 审计通过；E2E 随 3.0 |

## 3. 验证命令与结果

```bash
vue-tsc -b --pretty false        # 0 错误
vitest run                       # 33 文件 / 369 项全过（2.4 新增 5）
node scripts/audit-assets.mjs    # 通过，2.4 零新增托管视觉
npm run build                    # 成功
```

## 4. 有意不做（SOL-PLAN-2.4 §9）

- 不提前展示 2.5 完整后果或中点答案。
- 不为每个继承字段创建结构分支（仅 2 条 structural）。
- 不改 schema5、第一季模块或既有选择含义。
- 不生成图片，不用低差异视觉堆数量（本版零新增）。
