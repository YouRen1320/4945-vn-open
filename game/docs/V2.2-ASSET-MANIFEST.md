# 2.2 资产清单（V2.2-ASSET-MANIFEST）

> 关联：SOL-PLAN-2.2 §9.9；验收 `game/docs/V2.2-ACCEPTANCE.md`。
> 结论：**第二季共通序章（s2-prologue）零新增托管视觉资产即可游玩**，全部复用第一季既有背景、立绘与 CG。
> 资产审计命令：`node scripts/audit-assets.mjs`（结果：169 张母图全部有效，新引用均指向既有资产，无需新增）。

## 1. 序章复用的既有资产

### 背景（background 键 → 既有母图）
| 序章节点所用 | background 键 | 来源母图（已存在） | 用途 |
| --- | --- | --- | --- |
| 入口 / 回望变体 / 日常 | `organization` | 组织活动室母图 | 联合活动室 |
| 扰动 / 第一个选择 | `warRoom` | 作战室母图 | 北岸评议委员会播报 |
| 目标 / 收束 | `aftermath` | 余烬母图 | 新目标与帷幕拉开 |

### 立绘 / 角色（speaker / portrait）
| 角色键 | 复用说明 |
| --- | --- |
| `narrator` | 旁白（无立绘，引擎内置） |
| `player` | 玩家（无立绘，引擎内置） |
| `bottle` | 第一季既有角色立绘，复用 |
| `mentor` | 第一季既有角色立绘，复用 |
| `system` | 系统播报（无立绘，引擎内置） |

### CG
序章不引用任何新 CG；`historical` 字段统一标 `fictional`（架空剧情），沿用既有 background 占位与图鉴契约。

## 2. 新增资产需求

| 类别 | 需求 | 优先级 |
| --- | --- | --- |
| 背景 | 无（3 个键均为既有） | — |
| 立绘 / 表情 | 无（仅复用 bottle / mentor） | — |
| 告白 / 剧情 CG | 无（序章不触发 CG） | — |
| 标题页「第二季」专属美术 | 可选；当前沿用既有标题美术与「第二季共通序章」actLabel | 低 |

> 若后续 s2 集引入专属 CG / 背景，需在此表登记并追加 `V2.2-IMAGE-PROMPTS.md` 对应提示词，
> 且必须通过 `audit:assets:final`（要求全部就绪）后方可进入稳定发布。

## 3. 非破坏性原则

- 第二季不新增 `CharacterId`、不新增路线、不新增背景键。
- 所有 `s2-` 节点通过既有 `background` / `speaker` 复用，保证零运行时资源缺口。
- 既有 169 张母图、v1.8 托管视觉（4/36 已就绪，余有回退）与第二季无冲突。
