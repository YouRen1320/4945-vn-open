# V3.3.0 验收：事件三序章

## 完成范围

- 事件三独立进度、事件二 outcome 手动选择与 recap 建档。
- 五路线具体冻结损失、十五种关系承认、四种首次公开回应。
- 事件三序章完成状态与下一阶段目标。

## 自动验收

- `npm run typecheck`
- `npm test -- --run`
- `npm run build`
- 事件三内容矩阵：5 路线 × 15 关系状态 × 4 回应 = 300 条。
- 事件三来源/存储测试：独立槽、来源删除后续玩、多 outcome 不自动选择。

## 兼容与回滚

- 不改变 v4 事件一或 v5 事件二结构。
- 共享收藏 v2 会追加事件三已见节点与复用背景；这是跨季画廊的有意行为，不改写任何剧情状态。
- 新增键仅为 `4945-vn:s3:save:*`；回滚旧版本时该键保持但暂不可见，重新升级后恢复。
- 回顾建档作为明确标记的 S2 outcome 写入既有结局档案；真实 outcome 按 digest 幂等复用。

## 有意不做

- 不开放事件三后续集、季终结局、S4 或沈知行恋爱线。
- 本版复用现有背景，不把新头像/CG列为功能阻断项。
