# V2.9 验收清单

> 状态：**全部通过**（2026-08-06）。

## TypeCheck
- [x] `npm run typecheck` 零错误

## 单元测试
- [x] `npm test` 全通过（397 项，33 文件）

## 构建
- [x] `npm run build` 成功

## 资产审计
- [x] `npm run audit:assets` 通过（201 张正式母图，36/36 v1.8 托管视觉就绪）

## 语法合法性
- [x] 20 个节点全部通过 story/index.ts 的图验证（无断链/重复 ID/多出口）
- [x] s2-2.9-exit 在白名单中（`id.endsWith('-exit')` 规则自动覆盖）

## 条件路由
- [x] `s2MainEnding` StateVariableTarget 正确路由到 10 个尾声（9 case + 1 fallback）
- [x] `activePartner` 条件正确路由到 5 位伙伴 + fallback（无伴侣/未知伴侣）
- [x] fallback 伴侣走到 `s2-2.9-rel-none`

## 持久状态
- [x] `s2EpilogueEntered` flag 在进场后写入（s2-2.9-entry onEnter）
- [x] `s2EpilogueEnding` variable 写入正确的结局 ID（10 个尾声节点各写对应值）
- [x] `s2RelationshipResolved` variable 写入正确的伴侣 ID（5 伴侣 + 'none'）
- [x] `s2Complete` + `s2EpilogueComplete` flag 在总结节点写入
- [x] `s2-finale-complete` 结局 ID 写入 unlockedEndings

## 关系收束
- [x] 每位伙伴的信任 +2（shana/bottle/truth/yanqiu/wenxian）
- [x] 独行路径无关系效果（s2-2.9-rel-none 仅写 variable，无 relationship effect）

## 视觉
- [x] 所有节点 background 在 BackgroundId 枚举中存在
- [x] 所有节点 speaker/portrait 在 CharacterId 枚举中存在

## 无新增
- [x] 无新增 CharacterId
- [x] 无新增 BackgroundId
- [x] 无新增 RouteId / OrganizationId
- [x] 无新增 Schema 字段
- [x] 无新增 payoff ledger 条目（2.9 无玩家选择）

## 引擎变更
- [x] `activePartner` Condition 类型合法（types.ts:246）
- [x] `evaluateCondition` 正确处理 `activePartner`（state.ts:120-121）
